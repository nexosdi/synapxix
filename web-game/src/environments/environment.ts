export const environment = {
  apiUrl: 'http://localhost:3000/api', // Resolved for local dev
  auth0: {
    // TODO: Update these with real Auth0 credentials when available
    domain: '',
    clientId: '',
    audience: '',
  }
};