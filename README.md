# NexosDi Synapxix ✨

Adaptable education for all.

## Run projects

To run the dev server for your app, use:

```sh
npx nx serve server
```

To create a production bundle:

```sh
npx nx build web-game
```
